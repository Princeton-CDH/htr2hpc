coverage:
  status:
    project:
      default: false
      all:
        target: 57%
        paths:
          - "src/"
      tests:
        target: 100%
        paths:
          - "tests/"
    patch:
      default:
        target: 95%
